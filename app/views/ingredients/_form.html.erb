<%= form_with(model: ingredient) do |form| %>
  <% if ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
        <% ingredient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <% form.label :ingredients_name %>
    <%= form.text_field :ingredients_name, class:"form-control", placeholder:"Ingredient Name"  %>
  </div>

  <%= form_with(model: @ingredients_recipe) do |form| %>
    <div class="field form-group">
      <% form.label :ingredients_recipe_quantity %>
      <%= form.text_field :ingredients_recipe_quantity, class:"form-control", placeholder:"Quantity"  %>
    </div>
  <% end %>

  <div class="field form-group">
    <% form.label :ingredients_unit %>
    <%= form.text_field :ingredients_unit, class:"form-control", placeholder:"Unit"  %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-secondary'  %>
  </div>
<% end %>
